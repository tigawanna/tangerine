/** Max chars accepted by embed text (payload / paste comfort). */
export const EMBED_TEXT_MAX_CHARS = 20_000;

/** Soft UI max for word counter (EmbeddingGemma context ≈ 2048 tokens). */
export const EMBED_TEXT_MAX_WORDS = 2_048;
