# Client (Deno Desktop webview) — VITE_ prefixed
VITE_APP_URL=http://localhost:3070
# Auth + data API (apps/api). OAuth secrets live here — not in the desktop binary.
VITE_API_URL=http://localhost:5000
# Deno preload also reads this for token exchange. Must be the API, not :3070.
BETTER_AUTH_URL=http://localhost:5000
# System-browser sign-in page (apps/web). Desktop opens this with PKCE + loopback.
VITE_SIGN_IN_URL=http://localhost:3064/auth
# Stable OAuth loopback (default 17832). Random ports break after HMR / re-sign-in.
# DESKTOP_AUTH_LOOPBACK_PORT=17832

# PGlite NodeFS data directory (pgvector + live). Relative → under ~/.config/tangerine-desktop/
# Absolute paths used as-is. `memory://` for ephemeral in-memory DB.
DATABASE_URL=./pgdata

# Conveyor job queue (better-sqlite3). Defaults to ~/.config/tangerine-desktop/queue.db
# Plain path or `file:…` URL. QUEUE_DATABASE_PATH overrides QUEUE_DATABASE_URL.
# QUEUE_DATABASE_PATH=/path/to/queue.db
# QUEUE_DATABASE_URL=file:queue.db

# Optional: local EmbeddingGemma ONNX path (otherwise downloads from HuggingFace on first embed)
# GEMMA_MODEL_PATH=/path/to/embeddinggemma-300m-ONNX
# Quantization: q8 (default, ~300MB) | q4 (~200MB) | fp32 (~1.2GB — slow first download)
# GEMMA_DTYPE=q8

# PostHog product analytics (optional)
# VITE_POSTHOG_KEY=phc_...
# VITE_POSTHOG_HOST=https://us.i.posthog.com
