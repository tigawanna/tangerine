# Tangerine Desktop

Electron + [electron-vite](https://electron-vite.org/) shell with [Better Auth Electron](https://better-auth.com/docs/integrations/electron).

```bash
pnpm --filter electron dev
```

Copy `.env.example` → `.env`. API (`:5000`) and web (`:3064`) must be running for GitHub sign-in.
