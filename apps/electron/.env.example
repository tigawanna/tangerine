# Better Auth API (apps/api) — no GitHub secrets in the desktop app
MAIN_VITE_BETTER_AUTH_URL=http://localhost:5000

# Web sign-in page that runs `ensureElectronRedirect` + GitHub OAuth
MAIN_VITE_SIGN_IN_URL=http://localhost:3064/auth
