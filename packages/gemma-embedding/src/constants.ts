/** Value stored on `enriched_repos.model_id`. */
export const EMBEDDING_MODEL_ID = "embeddinggemma-300m";

/** Output width for EmbeddingGemma 300M — matches `F32_BLOB(n)` on Turso/libSQL. */
export const EMBEDDING_DIMENSIONS = 768;
